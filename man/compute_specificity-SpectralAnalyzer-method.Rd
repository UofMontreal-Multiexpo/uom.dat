% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spectral_analyzer.R
\name{compute_specificity,SpectralAnalyzer-method}
\alias{compute_specificity,SpectralAnalyzer-method}
\alias{compute_specificity}
\title{Specificity computation}
\usage{
\S4method{compute_specificity}{SpectralAnalyzer}(object, patterns, frequencies, weights)
}
\arguments{
\item{object}{S4 object of class \code{SpectralAnalyzer}.}

\item{patterns}{Patterns whose specificity is to be computed.}

\item{frequencies}{Vector of frequencies associated with the patterns contained in \code{patterns}.}

\item{weights}{Vector of weights associated with the patterns contained in \code{patterns}.}
}
\value{
Vector containing the specificity of each pattern contained in \code{patterns}.
}
\description{
Compute the specificity of the information conveyed by each pattern.
The specitificity corresponds to the nature of a pattern of being specific of a particular
combination or ubiquitous and allowing the formation of numerous combinations (with regard to the
observations).
}
\details{
\loadmathjax

The specificy of the pattern \eqn{p} is given by:
 \mjdeqn{S_p = \frac{\max_{q \in P} H_{max,q} - H_p}{\max_{q \in P} H_{max,q} - \min_{q \in P} H_{min,q}}}{S_p = (max H_max - H_p) / (max H_max - min H_min)}
where \eqn{P} is the set of patterns and:
\describe{
 \item{}{\mjeqn{H_p = - \sum_{v = 1}^{U_p} \frac{A_{p,v}}{W_p} \times ln \left( \frac{A_{p,v}}{W_p} \right)}{H_p = - sum(A_pv / W_p * ln(A_pv / W_p)) from v = 1 to U_p}}
 \item{}{\mjeqn{H_{max,p} = ln(U_p)}{H_max,p = ln(U_p)}}
 \item{}{\mjeqn{H_{min,p} = ln \left( \frac{W_p}{W_p - U_p + 1} \right) + \left( \frac{U_p - 1}{W_p} \times ln(W_p - U_p + 1) \right)}{H_min,p = ln(W_p / (W_p - U_p + 1)) + (U_p - 1) / W_p * ln(W_p - U_p + 1)}}
 \item{}{where \mjseqn{A_p} is the set of weights of the nodes containing the pattern \eqn{p},
         \eqn{W} is the set of weights of the patterns and
         \eqn{U} is the set of frequencies of the patterns.}
}
}
\references{
Bosson-Rieutort D, de Gaudemaris R, Bicout DJ (2018).
The spectrosome of occupational health problems. \emph{PLoS ONE} 13(1): e0190196.
\url{https://doi.org/10.1371/journal.pone.0190196}.
}
\author{
Gauthier Magnin
}
\keyword{internal}
